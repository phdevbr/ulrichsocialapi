{
    "version": 2,
    "buildCommand": "next build",
    "outputDirectory": "dist",
    "installCommand": "pnpm install",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "src/main.ts",
            "methods": ["GET", "POST", "PUT", "DELETE"]
        }
    ]
}
