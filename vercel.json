{
    "version": 2,
    "buildCommand": "pnpm build",
    "installCommand": "pnpm install",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/main.js",
            "methods": ["GET", "POST", "PUT", "DELETE"]
        }
    ]
}
